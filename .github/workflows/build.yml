name: Build for Appetize

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Select Xcode 16.1
        run: sudo xcode-select -s /Applications/Xcode_16.1.app/Contents/Developer

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode Project
        run: xcodegen generate

      - name: Build for iOS Simulator
        run: |
          xcodebuild -project HomePharmacy.xcodeproj \
                     -scheme HomePharmacy \
                     -sdk iphonesimulator \
                     -configuration Debug \
                     -derivedDataPath build_output \
                     clean build

      - name: Compress App Bundle
        run: |
          # Находим .app файл и копируем его прямо в текущую корневую папку
          APP_PATH=$(find build_output -name "HomePharmacy.app" -type d | head -n 1)
          echo "Копируем приложение из: $APP_PATH"
          cp -R "$APP_PATH" ./HomePharmacy.app
          
          # Архивируем приложение прямо из корня рабочей директории
          zip -r HomePharmacy.app.zip HomePharmacy.app

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: appetize-sim-build
          path: HomePharmacy.app.zip
